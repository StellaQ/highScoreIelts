<van-tabbar active="{{active}}" bind:change="onChange">
  <block wx:for="{{tabBarList}}" wx:key="text">
    <van-tabbar-item icon="{{item.icon}}" data-url="{{item.url}}">
      {{item.text}}
    </van-tabbar-item>
  </block>
</van-tabbar>